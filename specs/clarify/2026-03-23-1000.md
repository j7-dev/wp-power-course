# Clarify Session 2026-03-23 10:00

## Idea

當課程的觀看權限為「跟隨訂閱」時：
- 當訂閱狀態為「待取消」時，課程應該還是可以觀看
- 課程變為「已取消」跟「已逾期」時，才不能觀看

## Q&A

- Q: 修正方式選擇 — (A) 修正 ExpireDate::set_is_expired() 對齊 Course::is_expired()，統一 active + pending-cancel = 未過期 (B) 反過來修改 Course::is_expired()，pending-cancel 也視為過期 → A: 選擇 A，修正 ExpireDate::set_is_expired() 對齊，統一 active + pending-cancel = 未過期
- Q: 規格更新方式 — (A) 在「前置（狀態）- 課程存取未到期」Rule 下新增 2 個 Example：pending-cancel 仍可查詢 + cancelled 回傳 expired (B) 新增獨立 Rule → A: 選擇 A，在現有 Rule 下新增 2 個 Example
