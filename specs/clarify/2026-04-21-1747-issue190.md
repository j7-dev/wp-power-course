# Clarify Session 2026-04-21 17:47 — Issue #190「課程內頁 學員管理」新增學員優化

## Idea

> Issue #190 標題：課程內頁 學員管理
> Issue #190 內文：新增學員優化

> 使用者補充脈絡（摘錄）：
> 原本 `js/src/pages/admin/Courses/Edit/tabs/CourseStudents/UserSelector/index.tsx` L:123~L:156 這個
> 課程內學員管理的用戶選擇器，搜尋 users 的 API 是好的，繼續沿用就好了，為什麼要額外開發？
> 不要動後端，直接沿用現有 UserSelector 已經在用的 `students` resource
> （它已經支援 `meta_key=avl_course_ids` + `meta_value ne courseId` 的排除語意）。

## Q&A

- Q1（主）：users 排除已加入用戶的策略 → A：A（後端零改動，UserTable 在此場景切到 `students` resource + `meta_value ne courseId`）
- Q1a：UserTable 如何接受 resource 切換 → A：B（新增 `mode?: 'global' | 'course-exclude'` prop，預設 `'global'`）
- Q1b：`course-exclude` 模式的 permanent filter 處理 → A：B（各 mode 獨立定義自己的 permanent filter，完全覆寫）
- Q1c：`course-exclude` 下哪些子元件渲染 → A：A'（原推薦 A 修正版：Filter **完整保留三欄位**，其他 UI 全部隱藏；GrantCourseAccess / CSV 匯出 / HistoryDrawer / 批次到期日 都隱藏）
- Q2：批次加入 API → A：A（沿用 `POST /courses/add-students`，批次送 `user_ids[]` + `course_ids: [courseId]`，後端零改動）
- Q3（主）：加入時 `expire_date` 設定 UI → A：B（加入按鈕旁提供 DatePicker，預設不選代表永久 / expire_date = 0）
- Q3a：是否支援 `subscription_{id}` 格式 → A：A（只支援永久 + DatePicker → timestamp，不支援訂閱綁定）
- Q3b：DatePicker `disabledDate` → A：A（禁用今天以前，沿用 `Specific.tsx` 的 `current < dayjs().startOf('day')` 寫法）
- Q3c：DatePicker 作用對象 → A：A（批次統一到期日，套用到所有勾選 user_ids）
- Q4：加入成功後 invalidate 對象 → A：A（只 invalidate `students` resource list + reset `selectedUserIdsAtom` + 清空 DatePicker 值）
- Q5：Filter 面板保留哪些欄位 → A：C（完整保留 `search` + `avl_course_ids` + `include` 三欄位；同時觸發 Consistency Sweep 更新 Q1c = A'）
- Q6：分頁大小 → A：A（`pageSize: 20`，與全局 UserTable / StudentTable 一致，沿用 `getDefaultPaginationProps`）
- Q7：既有 `UserSelector/` 目錄處置 → A：A（直接刪除，`CourseStudents/index.tsx` 改用 `<UserTable mode="course-exclude" />`）
- Q7-延伸：「加入按鈕 + DatePicker + SelectedUser」由誰 render → 結論：由 UserTable 內部根據 `mode` 自行 render，不暴露 `actions` slot prop（封裝最乾淨）
- Q8：i18n 字串新增策略 → A：B（spec 列出新增 msgid 清單 + 直接寫好 `manual.json` 增量 JSON，master agent 不用自己推敲翻譯）

## Consistency Sweep 紀錄

- 2026-04-21 Q5 回覆後：Q1c 原結論「只留 keyword 搜尋」被 Q5（完整保留三欄位）推翻。更新為 A'：Filter 完整保留；其餘 UI（GrantCourseAccess / CSV 匯出 / HistoryDrawer / 批次到期日）維持隱藏。

## 最終結論（合併視圖）

| # | 題目 | 結論 |
|---|------|------|
| Q1 主 | users 排除已加入用戶策略 | A（後端零改動，UserTable 切到 `students` resource） |
| Q1a | UserTable 接受 resource 切換的方式 | B（`mode?: 'global' \| 'course-exclude'` prop） |
| Q1b | permanent filter 處理 | B（各 mode 獨立定義，完全覆寫） |
| Q1c | 子元件取捨 | A'（Filter 完整保留三欄位；其他全部隱藏） |
| Q2 | 批次 API | A（沿用 `POST /courses/add-students`） |
| Q3 主 | 加入時 expire_date UI | B（加 DatePicker 到按鈕旁，預設永久） |
| Q3a | 支援 subscription 格式 | A（否，只支援 timestamp + 永久） |
| Q3b | disabledDate | A（禁用今天以前，沿用 Specific.tsx 寫法） |
| Q3c | 批次 vs 逐用戶 | A（批次統一到期日） |
| Q4 | invalidate 對象 | A（只 invalidate `students` + reset atom + 清空 DatePicker） |
| Q5 | Filter 欄位 | C（完整保留 `search` + `avl_course_ids` + `include`） |
| Q6 | 分頁 | A（`pageSize: 20`） |
| Q7 | UserSelector 處置 | A（刪除整個目錄） |
| Q8 | i18n 策略 | B（spec 列出 msgid + `manual.json` 增量 JSON） |
