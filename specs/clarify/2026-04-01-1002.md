# 澄清紀錄 — 2026-04-01 10:02 (Issue #152 線性觀看模式)

## 澄清問題與用戶確認

| # | 問題 | 選項 | 用戶回答 | 確認內容 |
|---|------|------|---------|---------|
| Q1 | 後端鎖定執行層級 | A: Template+API 都擋 / B: 僅 Template / C: 僅前端 | **A** | Template 層（直接 URL）+ REST API 層雙重阻擋 |
| Q2 | 管理員/講師豁免 | A: 全部豁免 / B: 僅管理員 / C: 不豁免 | **A** | 管理員（manage_woocommerce）與講師（teacher_ids）均豁免 |
| Q3 | 管理端 UI 開關位置 | A: 其他設定 tab / B: 章節管理 tab / C: 新建 tab | **A** | 放在「其他設定」tab，與 enable_comment 等並列 |
| Q4 | 父層章節處理 | A: 只看葉節點 / B: 有影片才計入 / C: 所有章節都計入 | **C** | 所有章節不管父/子層都計入 DFS 展開序列 |
| Q5 | 前端鎖定互動方式 | A: 鎖頭+可點彈 Toast / B: 鎖頭+不可點 / C: 鎖頭+跳轉 | **A** | 顯示鎖頭圖示，可點擊但彈出 Toast 警告 |
| Q6 | 章節完成定義 | A: 沿用手動 / B: 增加自動 / C: 強制自動 | **A** | 沿用現有手動完成機制（toggle-finish-chapters API） |
| Q7 | Meta key 命名 | 用戶自訂 | `enable_linear_mode` | 值為 'yes'/'no'，預設 'no'，與現有 enable_comment 格式一致 |

## 實作狀態

經代碼審查，此功能已在當前分支**完整實作**：

### 後端 PHP
- `ChapterUtils::is_chapter_locked()` — 含管理員/講師豁免、DFS 序列判斷
- `ChapterUtils::get_locked_chapter_ids()` — 批量查詢，避免 N+1
- `ChapterUtils::get_flatten_post_ids()` — DFS 展開所有章節（含父子）
- `ChapterUtils::get_chapter_icon_html()` — 鎖頭圖示 + tooltip
- `ChapterUtils::get_children_posts_html_uncached()` — data-locked + 灰色文字
- `single-pc_chapter.php` line 58 — Template 層阻擋
- `404/locked.php` — 鎖定提示頁面
- `Chapter/Core/Api.php` — 章節 API 回傳 is_locked
- `Api/Course.php` — 課程 API 回傳 enable_linear_mode

### 前端 React
- `CourseOther/index.tsx` — FiSwitch 開關（label: 線性觀看模式）

### 前端 Vanilla JS
- `classroom/chapters.php` — data-locked 判斷、Toast 警告、3 秒自動消失

### 資料模型
- `enable_linear_mode` post meta ('yes'/'no', 預設 'no')
- 使用 `pc_avl_chaptermeta` 的 `finished_at` 追蹤完成狀態

## 結論

需求已完整澄清，代碼已實作完畢。接下來進入測試階段。
