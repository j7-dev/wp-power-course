# 管理員

## 描述
WordPress 後台操作者，負責建立課程、管理章節、設定外掛、管理學員與講師。擁有 `manage_options` 或 `edit_products` 能力（Capability）。

## 關鍵屬性
- WordPress 角色：`administrator` 或具有 `edit_products` 能力的自訂角色
- 操作範圍：所有課程、所有學員、全站設定
- 認證方式：WordPress 登入 Cookie + `X-WP-Nonce` Header
