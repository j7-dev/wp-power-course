# Clarify Session 2026-03-19 15:30

## Idea

修改 power-course 中所有圖片上傳方式，從現有的 FileUpload / OnChangeUpload（使用 Ant Design Upload + antd-img-crop，透過 form-data 上傳二進制檔案）改為使用 MediaLibraryModal（WordPress Media Library 選圖器），與 power-shop 的 Gallery 組件一致。

影響範圍：所有使用 `name={['files']}` 的地方，以及對應的 PHP API 端接收方式。

## Q&A

- Q: 修改範圍是僅課程封面圖，還是所有圖片上傳？ → A: B（所有使用 `name={['files']}` 的地方都要改，連同 API 都要參考 power-shop）
- Q: 用戶頭像上傳（UserAvatarUpload）也要改成 MediaLibraryModal 方式嗎？ → A: A（全部改，包含 UserAvatarUpload）
- Q: 郵件編輯器（EmailEditor）內的 onUploadImage 也要改嗎？ → A: B（不改，僅改表單欄位類型的圖片上傳）
- Q: 修改後是否需要支援多圖上傳？ → A: 直接推斷 — 課程封面圖限 1 張（limit=1），用戶頭像限 1 張（limit=1），與 power-shop Gallery 一致使用 limit 參數控制
