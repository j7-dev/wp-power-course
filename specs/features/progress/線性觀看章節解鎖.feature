@ignore @query
Feature: 取得線性觀看章節解鎖狀態

  線性觀看功能開啟時，學員必須依照章節排序順序完成前面的章節，才能觀看後面的章節。
  所有章節（含父章節與子章節）平攤為一維順序，按 menu_order 遞增判斷前後關係。
  解鎖演算法採「最遠進度模式」：以最遠已完成章節的位置為基準，
  其前方所有章節及其後第一個未完成章節均為解鎖狀態。

  Background:
    Given 系統中有以下用戶：
      | userId | name  | email          | role          |
      | 2      | Alice | alice@test.com | subscriber    |
      | 1      | Admin | admin@test.com | administrator |
    And 系統中有以下課程：
      | courseId | name       | _is_course | status  | enable_linear_viewing |
      | 100      | PHP 基礎課 | yes        | publish | yes                   |
    And 課程 100 有以下章節（平攤排序）：
      | chapterId | post_title      | post_parent | menu_order |
      | 200       | 1-1 PHP 簡介    | 100         | 1          |
      | 201       | 1-2 變數與型別  | 100         | 2          |
      | 202       | 1-3 條件判斷    | 100         | 3          |
      | 203       | 2-1 迴圈        | 100         | 4          |
    And 用戶 "Alice" 已被加入課程 100，expire_date 0

  # ========== 前置（狀態）==========

  Rule: 前置（狀態）- 課程未開啟線性觀看時，所有章節均為解鎖

    Example: 線性觀看關閉時所有章節可存取
      Given 課程 100 的 enable_linear_viewing 為 "no"
      When 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 200, 201, 202, 203 均為解鎖狀態

  Rule: 前置（狀態）- 管理員預覽模式免除線性觀看限制

    Example: 管理員預覽時所有章節可存取
      Given 用戶 "Admin" 為管理員且處於預覽模式
      When 查詢用戶 "Admin" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 200, 201, 202, 203 均為解鎖狀態

  Rule: 前置（狀態）- 學員必須已有該課程的存取權

    Example: 無課程存取權時查詢失敗
      Given 用戶 "Bob" 未被加入課程 100
      When 查詢用戶 "Bob" 在課程 100 的章節解鎖狀態
      Then 操作失敗，錯誤為「無此課程存取權」

  # ========== 後置（回應）- 解鎖演算法 ==========

  Rule: 後置（回應）- 第一個章節永遠解鎖，無完成紀錄時僅第一章節解鎖

    Example: 無任何完成紀錄時僅第一章節解鎖
      Given 用戶 "Alice" 未完成任何章節
      When 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 200 為解鎖狀態
      And 章節 201, 202, 203 為鎖定狀態

  Rule: 後置（回應）- 以最遠已完成章節位置為基準，位置 0 到 (最遠位置+1) 為解鎖

    Example: 完成第一章節後下一章節解鎖
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      When 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 200, 201 為解鎖狀態
      And 章節 202, 203 為鎖定狀態

    Example: 連續完成兩章後第三章節解鎖
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      And 用戶 "Alice" 在章節 201 的 finished_at 為 "2025-06-02 10:00:00"
      When 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 200, 201, 202 為解鎖狀態
      And 章節 203 為鎖定狀態

    Example: 完成所有章節後全部解鎖
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      And 用戶 "Alice" 在章節 201 的 finished_at 為 "2025-06-02 10:00:00"
      And 用戶 "Alice" 在章節 202 的 finished_at 為 "2025-06-03 10:00:00"
      And 用戶 "Alice" 在章節 203 的 finished_at 為 "2025-06-04 10:00:00"
      When 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 200, 201, 202, 203 均為解鎖狀態

  Rule: 後置（回應）- 最遠進度模式忽略中間完成缺口

    Example: 跳躍完成時以最遠完成為基準
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      And 用戶 "Alice" 在章節 202 的 finished_at 為 "2025-06-02 10:00:00"
      # 最遠完成 = 202（位置 2），解鎖到位置 3
      When 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 200, 201, 202, 203 均為解鎖狀態

    Example: 僅完成非首章節時以該章節為最遠基準
      Given 用戶 "Alice" 在章節 202 的 finished_at 為 "2025-06-02 10:00:00"
      # 最遠完成 = 202（位置 2），解鎖到位置 3
      When 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 200, 201, 202, 203 均為解鎖狀態

  # ========== 後置（回應）- 章節排序與巢狀結構 ==========

  Rule: 後置（回應）- 含巢狀子章節時按平攤順序判斷

    Example: 父子章節平攤後按 menu_order 判斷解鎖
      Given 課程 100 有以下章節（平攤排序）：
        | chapterId | post_title   | post_parent | menu_order |
        | 200       | 第一章       | 100         | 1          |
        | 204       | 1-1 子章節A  | 200         | 1          |
        | 205       | 1-2 子章節B  | 200         | 2          |
        | 201       | 第二章       | 100         | 2          |
      And 用戶 "Alice" 在章節 204 的 finished_at 為 "2025-06-01 10:00:00"
      # 平攤順序: 200 → 204 → 205 → 201，最遠完成 = 204（位置 1），解鎖到位置 2
      When 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 200, 204, 205 為解鎖狀態
      And 章節 201 為鎖定狀態

  # ========== 後置（回應）- 取消完成的連鎖效果 ==========

  Rule: 後置（回應）- 取消最遠完成章節後重新計算解鎖邊界

    Example: 取消最遠完成章節後後續章節重新鎖定
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      And 用戶 "Alice" 在章節 201 的 finished_at 為 "2025-06-02 10:00:00"
      And 用戶 "Alice" 在章節 202 的 finished_at 為 "2025-06-03 10:00:00"
      # 取消 202 後，最遠完成 = 201（位置 1），解鎖到位置 2
      When 用戶 "Alice" 取消章節 202 的完成狀態
      And 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 章節 200, 201, 202 為解鎖狀態
      And 章節 203 為鎖定狀態

    Example: 取消非最遠完成章節不影響解鎖邊界
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      And 用戶 "Alice" 在章節 201 的 finished_at 為 "2025-06-02 10:00:00"
      And 用戶 "Alice" 在章節 202 的 finished_at 為 "2025-06-03 10:00:00"
      # 取消 201（非最遠），最遠仍為 202（位置 2），解鎖到位置 3
      When 用戶 "Alice" 取消章節 201 的完成狀態
      And 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 章節 200, 201, 202, 203 均為解鎖狀態

  # ========== 後置（回應）- 鎖定提示資訊 ==========

  Rule: 後置（回應）- 鎖定章節附帶當前需完成章節的名稱與 ID

    Example: 鎖定章節提示當前應完成的章節名稱
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      # 解鎖到位置 1（201），鎖定位置 2（202）及之後
      # 當前需完成 = 201「1-2 變數與型別」
      When 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 202 的鎖定提示應包含章節名稱「1-2 變數與型別」
      And 章節 203 的鎖定提示應包含章節名稱「1-2 變數與型別」

    Example: 無完成紀錄時鎖定提示指向第一章節
      Given 用戶 "Alice" 未完成任何章節
      When 查詢用戶 "Alice" 在課程 100 的章節解鎖狀態
      Then 操作成功
      And 章節 201 的鎖定提示應包含章節名稱「1-1 PHP 簡介」

  # ========== 後置（回應）- 直接 URL 存取鎖定章節 ==========

  Rule: 後置（回應）- 直接 URL 存取鎖定章節時導向當前應觀看章節

    Example: 透過 URL 存取鎖定章節被 redirect 到當前應觀看章節
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      # 當前應觀看 = 201（第一個解鎖且未完成的章節）
      When 用戶 "Alice" 透過 URL 直接存取章節 203
      Then 被導向章節 201 的頁面
      And 頁面顯示提示訊息「請先完成前面的章節才能觀看此內容」

    Example: 透過 URL 存取已解鎖章節正常顯示
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      When 用戶 "Alice" 透過 URL 直接存取章節 201
      Then 正常顯示章節 201 的內容頁面

  # ========== 後置（回應）- 教室 UI 行為 ==========

  Rule: 後置（回應）- 章節導航列表中鎖定章節顯示鎖定圖示與提示

    Example: 鎖定章節在側邊欄顯示鎖頭圖示
      Given 用戶 "Alice" 未完成任何章節
      When 用戶 "Alice" 進入課程 100 的教室頁面
      Then 章節 200 在側邊欄顯示為可點擊狀態
      And 章節 201, 202, 203 在側邊欄顯示鎖定圖示
      And 鎖定章節顯示提示文字「請先完成『1-1 PHP 簡介』才能觀看本章節」

  Rule: 後置（回應）- 點擊鎖定章節顯示友善提示而非導航

    Example: 點擊側邊欄鎖定章節顯示提示訊息
      Given 用戶 "Alice" 未完成任何章節
      When 用戶 "Alice" 在側邊欄點擊鎖定的章節 202
      Then 顯示提示訊息「請先完成『1-1 PHP 簡介』才能觀看本章節」
      And 不導航到章節 202 的頁面

  Rule: 後置（回應）- 底部「下一個」按鈕在下一章節鎖定時顯示但禁用

    Example: 下一章節鎖定時按鈕灰色不可點擊
      Given 用戶 "Alice" 未完成任何章節
      When 用戶 "Alice" 在章節 200 的頁面
      Then 底部「下一個」按鈕顯示但呈灰色不可點擊
      And 按鈕附帶提示文字「完成本章節後即可觀看下一章」

    Example: 下一章節已解鎖時按鈕正常可點擊
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      When 用戶 "Alice" 在章節 200 的頁面
      Then 底部「下一個」按鈕正常可點擊，連結到章節 201

  Rule: 後置（回應）- 影片播完時下一章節鎖定則不自動跳轉

    Example: 影片播完但下一章節鎖定時顯示完成提示
      Given 用戶 "Alice" 未完成任何章節
      When 用戶 "Alice" 在章節 200 的影片播放結束
      Then 不自動跳轉到下一章節
      And 顯示提示「請點擊『完成章節』以解鎖下一章」

  # ========== 後置（回應）- 即時前端更新 ==========

  Rule: 後置（回應）- 完成章節後前端即時解鎖下一章節（不重新載入頁面）

    Example: 完成章節後側邊欄即時更新解鎖狀態
      Given 用戶 "Alice" 未完成任何章節
      And 用戶 "Alice" 在章節 200 的頁面
      When 用戶 "Alice" 點擊「完成章節」按鈕
      Then API 回應包含更新後的 unlocked_chapter_ids
      And 側邊欄章節 201 的鎖定圖示即時消失，變為可點擊狀態
      And 底部「下一個」按鈕即時變為可點擊狀態
      And 頁面不重新載入

    Example: 取消完成章節後前端即時重新鎖定
      Given 用戶 "Alice" 在章節 200 的 finished_at 為 "2025-06-01 10:00:00"
      And 用戶 "Alice" 在章節 200 的頁面
      And 章節 201 目前為解鎖狀態
      When 用戶 "Alice" 點擊「取消完成」按鈕
      Then API 回應包含更新後的 unlocked_chapter_ids
      And 側邊欄章節 201 即時顯示鎖定圖示
      And 底部「下一個」按鈕即時變為灰色不可點擊
      And 頁面不重新載入
