@ignore @ui
Feature: 教室前端章節鎖定 UI

  開啟線性觀看的課程教室頁面中，前端顯示章節的鎖定/解鎖狀態。
  包括側邊欄章節列表的鎖頭圖示、點擊鎖定章節的提示訊息、
  「前往下一章節」按鈕的禁用/啟用狀態、以及「完成章節」後的即時解鎖效果。

  Background:
    Given 課程「PHP 基礎課」已開啟線性觀看模式
    And 課程有以下章節（攤平順序）：第一章 → 1-1 → 1-2 → 第二章 → 2-1

  # ========== 側邊欄章節列表 ==========

  Rule: 鎖定章節在側邊欄顯示鎖頭圖示

    Example: 新學員看到的章節列表
      Given 學員未完成任何章節
      When 學員進入教室頁面
      Then 側邊欄章節列表中：
        | 章節   | 圖示   | 可點擊 |
        | 第一章 | 正常   | 是     |
        | 1-1    | 鎖頭🔒 | 否     |
        | 1-2    | 鎖頭🔒 | 否     |
        | 第二章 | 鎖頭🔒 | 否     |
        | 2-1    | 鎖頭🔒 | 否     |

    Example: 已完成第一章和 1-1 的學員
      Given 學員已完成第一章和 1-1
      When 學員進入教室頁面
      Then 側邊欄章節列表中：
        | 章節   | 圖示   | 可點擊 |
        | 第一章 | 打勾✅ | 是     |
        | 1-1    | 打勾✅ | 是     |
        | 1-2    | 正常   | 是     |
        | 第二章 | 鎖頭🔒 | 否     |
        | 2-1    | 鎖頭🔒 | 否     |

  # ========== 點擊鎖定章節 ==========

  Rule: 點擊鎖定章節顯示友善提示

    Example: 學員點擊被鎖定的章節
      Given 學員在第一章教室頁面
      And 1-1 為鎖定狀態
      When 學員點擊側邊欄的 1-1
      Then 顯示提示訊息「請先完成前面的章節後再觀看此章節」
      And 頁面不跳轉，仍停留在當前章節

  # ========== 完成章節按鈕 ==========

  Rule: 線性觀看模式下隱藏「標示為未完成」功能

    Example: 已完成章節的完成按鈕
      Given 學員已完成第一章
      And 學員在第一章教室頁面
      When 頁面載入完畢
      Then 完成按鈕顯示為已完成樣式（outline 樣式）
      And 按鈕文字為「已完成」
      And 按鈕為不可點擊狀態（禁止取消完成）

    Example: 未完成章節的完成按鈕
      Given 學員在第一章教室頁面（未完成）
      When 頁面載入完畢
      Then 完成按鈕顯示為「標示為已完成」
      And 按鈕可點擊

  # ========== 「前往下一章節」按鈕 ==========

  Rule: 未完成當前章節時「前往下一章節」按鈕禁用

    Example: 當前章節未完成
      Given 學員在第一章教室頁面（未完成）
      When 頁面載入完畢
      Then 「前往下一章節」按鈕為禁用狀態（灰色、不可點擊）
      And hover 時顯示 tooltip「請先完成當前章節」

  Rule: 完成當前章節後「前往下一章節」按鈕即時啟用

    Example: 學員完成章節後按鈕即時可用
      Given 學員在第一章教室頁面（未完成）
      And 「前往下一章節」按鈕為禁用狀態
      When 學員點擊「標示為已完成」按鈕
      And API 回應成功
      Then 「前往下一章節」按鈕即時變為可點擊狀態（主色調）
      And 按鈕連結指向 1-1 的 URL
      And 頁面不重新載入

  # ========== 即時解鎖（JS） ==========

  Rule: 完成章節後側邊欄即時更新

    Example: 完成第一章後側邊欄即時反映
      Given 學員在第一章教室頁面（未完成）
      And 側邊欄 1-1 顯示鎖頭圖示
      When 學員點擊「標示為已完成」按鈕
      And API 回應成功
      Then 第一章的圖示變為打勾✅
      And 1-1 的鎖頭圖示消失，變為正常圖示（可觀看）
      And 1-1 變為可點擊
      And 1-2 仍為鎖頭圖示

  # ========== 管理員預覽 ==========

  Rule: 管理員看到鎖定 UI 但所有章節可存取

    Example: 管理員預覽教室頁面
      Given 管理員以預覽模式進入教室
      When 頁面載入完畢
      Then 側邊欄顯示鎖頭圖示（模擬學員視角）
      But 所有章節均可點擊存取
      And 完成按鈕正常顯示（管理員可自由操作）
