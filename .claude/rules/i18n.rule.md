---
paths:
  - "inc/**/*.php"
  - "plugin.php"
  - "js/src/**/*.ts"
  - "js/src/**/*.tsx"
  - "inc/assets/src/**/*.ts"
  - "inc/assets/src/**/*.tsx"
  - "scripts/i18n-translations/**/*.json"
  - "scripts/i18n-make-pot.mjs"
  - "scripts/build-zhtw-po.mjs"
---

# Power Course i18n（國際化）規範

本規範定義 Power Course 外掛在多語系支援上的強制規則。所有 PHP 與 React 程式碼必須遵守。違規視為 PR 審查不通過。

詳細 API 用法請參考 `zenbu-powers:wordpress-i18n` skill。

## 核心決策

| 項目 | 決策 |
|------|------|
| Text Domain | **固定 `power-course`**（連字號，與 `plugin.php` header 一致） |
| Domain Path | `/languages` |
| **msgid 原文語言** | **英文完整句子**（WP 官方慣例）；繁體中文走 `zh_TW.po` 的 `msgstr` |
| 翻譯來源 | 單一 `.po/.mo`，PHP 與 React **共用** |
| PHP API | WordPress 原生 `__()` / `_e()` / `_x()` / `_n()` 等 |
| React API | **`@wordpress/i18n`**（已安裝），**禁止**使用 `i18next` / `react-intl` |
| 工具鏈 | `pnpm run i18n:build` 一次跑完 `pot → merge → mo → json`（詳見「工具鏈使用規範」節） |
| 翻譯來源檔 | `scripts/i18n-translations/manual.json`（Single Source of Truth；禁止手改 `.po`） |
| 目標語言 | zh_TW（主要介面語言）+ en_US（fallback）+ ja（次階段） |

---

## 🔴 最高原則：msgid 一律是英文

WordPress i18n 的核心假設是 **msgid = 英文原文 / msgstr = 各語言翻譯**。

- ✅ `__( 'Course name', 'power-course' )`
- ❌ `__( '課程名稱', 'power-course' )`（違反 WP 慣例，非 zh_TW 站點 fallback 會顯示中文）

**理由：**

1. 英文 msgid 讓所有語系譯者（日文、西文、法文…）都看得懂原文。
2. `.po` 編輯器（Poedit, Loco Translate）與翻譯平台（GlotPress, Weblate）預設把 msgid 當英文。
3. WordPress core / WooCommerce / 主流外掛全部使用英文 msgid。
4. 當 `.mo` 載入失敗時，fallback 會直接顯示 msgid — 顯示英文遠比顯示中文對全球使用者友善。
5. `make-pot` 掃描出的字串交給外部譯者時，英文是唯一共通語言。

**translator comments 仍使用繁體中文**（因為譯者是繁中開發團隊）：

```php
sprintf(
    /* translators: %s: 課程名稱 */   ← 繁體中文，給譯者看
    __( 'Course "%s" created', 'power-course' ),   ← 英文 msgid
    $course_name
)
```

---

## 術語表（Glossary）

為避免不同檔案用字分歧，**必須**使用下表的統一譯法。新詞彙請先加到本表再套用。

| 繁體中文 | 英文 msgid 標準譯法 |
|---------|-----------------------|
| 課程 | Course |
| 章節 | Chapter |
| 小節 / 單元 | Lesson |
| 學員 | Student |
| 講師 | Instructor |
| 銷售方案 / Bundle | Bundle |
| 購物車 | Cart |
| 訂單 | Order |
| 商品 | Product |
| 教室 | Classroom |
| 觀看時間 | Watch time |
| 觀看進度 | Watch progress |
| 開課時間 | Course start time |
| 課程時長 | Course duration |
| 章節數量 | Chapter count |
| 學員人數 | Student count |
| 加入購物車 | Add to cart |
| 立即報名 | Enroll now |
| 立即購買 | Buy now |
| 前往教室 | Go to classroom |
| 前往課程 | Visit course |
| 儲存 | Save |
| 刪除 | Delete |
| 建立 | Create |
| 新增 | Add |
| 編輯 | Edit |
| 取消 | Cancel |
| 確認 | Confirm |
| 確定刪除 | Confirm delete |
| 關閉 | Close |
| 關閉側邊欄 | Close sidebar |
| 載入中 | Loading |
| 影片載入中... | Loading video... |
| 未設定 | Not set |
| 評論 | Review |
| 評分 | Rating |
| 標籤 | Tag |
| 熱門 | Popular |
| 精選 | Featured |
| 已加入 | Joined |
| 課程已全數上架完畢 | All chapters published |
| 請選擇課程 | Please select a course |
| 文章不存在 | Post does not exist |
| 產品不存在 | Product does not exist |
| 複製文章失敗 | Failed to duplicate post |
| 刪除課程資料失敗 | Failed to delete course data |
| 刪除章節資料失敗 | Failed to delete chapter data |
| 刪除電子郵件資料失敗 | Failed to delete email data |
| 您已經購買課程 | You have already purchased this course |
| 回《我的課程》 | Back to My Courses |
| by %s | by %s |
| 重看本章 | Replay chapter |
| 下個章節將在 %d 秒後自動播放（單數） | Next chapter will play in %d second |
| 下個章節將在 %d 秒後自動播放（複數） | Next chapter will play in %d seconds |

**語法慣例：**

- 完整句子首字大寫（Sentence case），而非 Title Case：`'Add to cart'` ✅，`'Add To Cart'` ❌
- 錯誤訊息用「Failed to + 動詞原形」：`'Failed to delete course'` ✅，`'Delete course failed'` ❌
- 動作按鈕用動詞原形：`'Save'` / `'Delete'` / `'Cancel'`

---

## PHP 規範

### 1. 必須使用翻譯函式

任何**使用者可見的字串**（UI 標籤、訊息、錯誤、按鈕、placeholder、admin 通知、tab 標題）必須包在翻譯函式內，禁止硬編碼。

```php
// ✅ 正確
__( 'Course name', 'power-course' )
esc_html__( 'Failed to delete course data', 'power-course' )

// ❌ 錯誤
'Course name'  // 硬編碼，無法翻譯
__( 'Course name' )  // 缺 text domain，make-pot 抓不到
__( 'Course name', 'power_course' )  // domain 錯：必須是連字號
__( '課程名稱', 'power-course' )  // msgid 必須是英文
```

### 2. 強制使用 escape 變體輸出 HTML

直接輸出到 HTML 的字串必須用 escape 版本，**禁止裸用 `_e()` 輸出**：

| 場景 | 必用函式 |
|------|---------|
| 輸出到 HTML body | `esc_html_e()` / `esc_html__()` |
| 輸出到 HTML attribute（`title="..."` / `aria-label="..."`） | `esc_attr_e()` / `esc_attr__()` |
| 輸出到 URL 屬性（`href` / `src`） | `esc_url()` 包外層 |
| 輸出到 JS context | `esc_js()` 包外層 |
| 含 HTML tag 必要的字串 | `wp_kses_post()` 包外層 |

```php
// ✅ 正確
echo esc_html__( 'Please select a course', 'power-course' );
?>
<button title="<?php esc_attr_e( 'Add to cart', 'power-course' ); ?>"></button>

// ❌ 錯誤
echo __( 'Please select a course', 'power-course' );  // XSS 風險
_e( 'Please select a course', 'power-course' );  // 同上
```

### 3. text domain 必須是字串字面量

`make-pot` 是**靜態掃描**，禁止把 text domain 放在變數內：

```php
// ✅ 正確
__( 'Course name', 'power-course' )

// ❌ 錯誤：make-pot 掃不到，永遠無法翻譯
$domain = 'power-course';
__( 'Course name', $domain )
```

### 4. sprintf + placeholder 規範

帶變數的字串**必須**用 `sprintf` + placeholder，禁止字串拼接：

```php
// ✅ 正確
sprintf(
    /* translators: %s: 課程名稱 */
    esc_html__( 'Course "%s" created', 'power-course' ),
    esc_html( $course_name )
);

// 多參數時必須用編號 placeholder（譯者可重排序）
sprintf(
    /* translators: 1: 學員姓名, 2: 課程名稱 */
    esc_html__( '%1$s joined course "%2$s"', 'power-course' ),
    esc_html( $student_name ),
    esc_html( $course_name )
);

// ❌ 錯誤：字串拼接
__( 'Course "', 'power-course' ) . $course_name . __( '" created', 'power-course' )

// ❌ 錯誤：直接代換變數
__( "Course \"{$course_name}\" created", 'power-course' )  // make-pot 掃到的字串永遠變動
```

### 5. 複數規範

數量相關的字串必須用 `_n()`：

```php
// ✅ 正確
sprintf(
    /* translators: %d: 學員人數 */
    _n(
        '%d student',
        '%d students',
        $count,
        'power-course'
    ),
    $count
);

// ❌ 錯誤
sprintf( __( '%d students', 'power-course' ), $count );
```

### 6. translator comments 慣例

含 placeholder 的字串**必須**在前一行加 `/* translators: ... */` 註解，**使用繁體中文**說明 placeholder 的意義：

```php
sprintf(
    /* translators: 1: 折扣百分比, 2: 課程名稱 */
    esc_html__( 'Get %1$d%% off course "%2$s"', 'power-course' ),
    $discount,
    esc_html( $course_name )
);
```

---

## React / TypeScript 規範

### 1. 必須使用 `@wordpress/i18n`

```tsx
// ✅ 正確
import { __, _x, _n, sprintf } from '@wordpress/i18n';

<Button>{__('Save course', 'power-course')}</Button>

// ❌ 錯誤：使用 i18next
import { useTranslation } from 'react-i18next';

// ❌ 錯誤：硬編碼
<Button>儲存課程</Button>

// ❌ 錯誤：中文 msgid
<Button>{__('儲存課程', 'power-course')}</Button>
```

### 2. text domain 必須與 PHP 一致

所有 JS 翻譯呼叫的第二參數**必須是 `'power-course'`**（與 PHP 共用）。`wp_set_script_translations` 已在 `inc/classes/Bootstrap.php` 接好，runtime 會自動載入對應翻譯。

### 3. Ant Design 元件文字

Ant Design 的 `label`、`placeholder`、`title`、`tooltip`、`okText`、`cancelText` 等 props 都是使用者可見字串，必須翻譯：

```tsx
// ✅ 正確
<Form.Item
  label={__('Course name', 'power-course')}
  name="name"
>
  <Input placeholder={__('Please enter course name', 'power-course')} />
</Form.Item>

<Modal
  title={__('Confirm delete', 'power-course')}
  okText={__('Confirm delete', 'power-course')}
  cancelText={__('Cancel', 'power-course')}
/>
```

### 4. sprintf 規範（同 PHP）

```tsx
// ✅ 正確
import { sprintf, __ } from '@wordpress/i18n';

const message = sprintf(
  // translators: %s: 課程名稱
  __('Course "%s" created', 'power-course'),
  courseName
);

// 多參數
const message = sprintf(
  // translators: 1: 學員姓名, 2: 課程名稱
  __('%1$s joined course "%2$s"', 'power-course'),
  studentName,
  courseName
);

// ❌ 錯誤：模板字串
const message = `課程「${courseName}」已建立`;

// ❌ 錯誤：直接拼接
const message = __('Course "', 'power-course') + courseName + __('" created', 'power-course');
```

### 5. React 元素帶在翻譯字串內

含 React 元素（如 `<a>`、`<strong>`）的翻譯，使用 `createInterpolateElement`：

```tsx
import { createInterpolateElement } from '@wordpress/element';
import { __ } from '@wordpress/i18n';

createInterpolateElement(
  __('Please read <a>terms of service</a> and agree', 'power-course'),
  {
    a: <a href="/terms" />,
  }
);
```

---

## 工具鏈使用規範

### Pipeline 全景

```
原始碼                         [pnpm run i18n:build 串全套]
──────                         ────────────────────────
PHP (inc/)                          │
JS/TSX (js/src/)                    │  i18n:pot   ──→ languages/power-course.pot
JS (inc/assets/src/)                │                 （英文 msgid 總表）
                                    │
scripts/i18n-translations/          │  i18n:merge ──→ languages/power-course-zh_TW.po
  └── manual.json    ◄──────────────┤                 （build-zhtw-po.mjs：.pot + 既有 .po + 對照表合併）
                                    │
                                    │  i18n:mo    ──→ languages/power-course-zh_TW.mo
                                    │                 （PHP runtime 讀）
                                    │
                                    └  i18n:json  ──→ languages/power-course-zh_TW.json
                                                      （JS runtime 讀，透過 inject_locale_data_to_handle）
```

### 新增/修改字串後的完整流程

**步驟 1. 寫程式**

在 PHP / JSX / TSX / TS 中用 `__()` / `_e()` / `esc_html__()` 等 WP i18n 函式包字串，msgid 一律英文（見「🔴 最高原則」）。

**步驟 2. 補繁中翻譯到 `scripts/i18n-translations/manual.json`**

新字串的繁中翻譯**必須**寫進此對照表，**禁止手改 `.po`**。理由：

- `build-zhtw-po.mjs` 每次重建 `.po` 時會**全量覆寫**，手寫進 `.po` 的 msgstr 在下次跑 pipeline 時會遺失
- 對照表是唯一的「翻譯真相來源」（Single Source of Truth）
- PR review 時翻譯變動集中在一個 JSON diff，易於審閱

格式（純 array 最直觀，其他支援格式詳見 `scripts/build-zhtw-po.mjs::extractList()`）：

```json
[
  {
    "msgid": "Featured course",
    "msgstr_zh_TW": "精選課程",
    "context": "inc/templates/components/badge/feature.php"
  }
]
```

**步驟 3. 跑 pipeline**

```bash
pnpm run i18n:build
# = pnpm run i18n:pot && pnpm run i18n:merge && pnpm run i18n:mo && pnpm run i18n:json
```

**步驟 4. 檢查輸出**

- console 印出的 `[build-zhtw-po] 未翻譯（空 msgstr）: N` 應該越少越好；非 plugin header / debug 訊息類不能有空白
- `git diff languages/` 確認 `.pot` / `.po` / `.mo` / `.json` 四個檔都有預期字串

### 細分指令

| 指令 | 做什麼 | 何時單獨跑 |
|---|---|---|
| `pnpm run i18n:pot` | 掃原始碼 → `.pot`（WP-CLI + gettext-extractor） | 只想確認新字串有被抽到 |
| `pnpm run i18n:merge` | `.pot` + 既有 `.po` + `manual.json` → 新 `.po` | 只補了翻譯，`.pot` 沒變 |
| `pnpm run i18n:mo` | `.po` → `.mo`（PHP runtime） | 只改了 `.po` |
| `pnpm run i18n:json` | `.po` → JED JSON（JS runtime） | 同上 |

### 檔名慣例

- `languages/power-course.pot`：英文 msgid 總表
- `languages/power-course-{locale}.po`：逐語系翻譯（目前只有 `zh_TW`）
- `languages/power-course-{locale}.mo`：PHP runtime 用（binary）
- `languages/power-course-{locale}.json`：JS runtime 用（JED 格式）
  - **注意**：本專案刻意**不使用** WP 預設的 `{domain}-{locale}-{handle-md5}.json` 檔名，而是產單一 JSON 後透過 `inject_locale_data_to_handle()` 注入到各 script bundle 執行前。詳見 `inc/classes/Bootstrap.php` 與 `inc/classes/Templates/Ajax.php`。

### 掃描範圍（JS/TS）

`scripts/i18n-make-pot.mjs` 的 `JS_GLOBS` 定義掃描路徑：

- `js/src/**` — Admin React SPA（handle: `Plugin::$kebab`）
- `inc/assets/src/**` — 前台 vanilla TS，jQuery-based（handle: `Plugin::$kebab . '-template'`）

若新增其他 JS/TS 進入點目錄，**必須同步擴充此陣列**，並確認 script handle 有呼叫 `wp_set_script_translations()` + `inject_locale_data_to_handle()`，否則字串永遠不會被翻譯。

---

## PR 審查驗收標準

新 PR 提交時，`wordpress-reviewer` 與 `react-reviewer` 必須檢查：

1. ✅ **msgid 全部是英文完整句子**，無中日韓字元
2. ✅ 新增/修改的使用者可見字串都用翻譯函式包裹（`__`, `_e`, `esc_html__` 等）
3. ✅ text domain 一律是字串字面量 `'power-course'`，無變數、無底線版本（`'power_course'` 是 bug）
4. ✅ HTML 輸出用 escape 變體（`esc_html_e` / `esc_html__` / `esc_attr_e` / `esc_attr__`）
5. ✅ 含變數的字串用 `sprintf` + placeholder，不是字串拼接或模板字串
6. ✅ 含 placeholder 的字串前有 `/* translators: ... */` 註解（繁體中文）
7. ✅ 複數場景用 `_n()` 而非 `sprintf( __('%d ...'), $count )`
8. ✅ React 端 import 來自 `@wordpress/i18n`，不是 `i18next` 或 `react-intl`
9. ✅ Ant Design 的 label/placeholder/title/tooltip/okText 都已翻譯
10. ✅ 若新詞彙不在術語表，已先 PR 更新術語表
11. ✅ 跑過 `pnpm run i18n:build`（**不是**只跑 `i18n:pot`），新字串已出現在 `languages/power-course.pot`
12. ✅ 新字串的繁中翻譯已加到 `scripts/i18n-translations/manual.json`（**禁止手改 `.po`**，會被 pipeline 覆寫）
13. ✅ `languages/power-course-zh_TW.po` / `.mo` / `.json` 四個檔案的 diff 都已一起 commit
14. ✅ 若新增了 JS/TS 進入點目錄，已同步更新 `scripts/i18n-make-pot.mjs` 的 `JS_GLOBS`，並確認 script handle 有接 `wp_set_script_translations()`
15. ✅ `[build-zhtw-po] 未翻譯（空 msgstr）` 的數量沒有因本 PR 上升（plugin header / debug / 中文 msgid 除外）
16. ✅ 若新增了 Vite config 或 entry point，`resolve.alias` 已加 `@wordpress/i18n` → `js/src/shims/wordpress-i18n.ts`，否則 bundle 內 `__()` 讀不到 runtime 翻譯（見「前台 bundle 漏掛 @wordpress/i18n shim」）

不通過則 reviewer 退回給 master agent 修正。

---

## 已知歷史問題

### Domain 名稱不一致（vendor，已修復）

`vendor/j7-dev/wp-plugin-trait` **0.2.19 及更早版本** 的 `PluginTrait::load_textdomain()` 會用 snake_case 呼叫 `load_plugin_textdomain('power_course', ...)`，與 plugin.php header 的 kebab-case `power-course` 不一致，導致翻譯載入到錯誤 domain。

**修復版本**：`j7-dev/wp-plugin-trait` **>= 0.2.20**（2026-04 發布）已改為使用 `self::$kebab`，正確載入 `power-course` domain。`composer.lock` 必須至少為此版本。

**禁止**動 vendor 內程式碼。

### WooCommerce text domain 借用（已豁免）

`inc/templates/components/review/comment_form.php` 的 `title_reply` 與 `label_submit` 欄位使用 `'woocommerce'` text domain，刻意借用 WooCommerce 現成翻譯（"Add a review"、"Submit"），以共享各語言的 WC 翻譯資源。

**豁免理由**：減少翻譯維護成本，且 WooCommerce 的翻譯品質在主流語言中已被驗證。

**限制範圍**：僅限 `comment_form.php` 這個 WC 整合介面的 `comment_form()` 參數；其餘所有模板一律使用 `'power-course'` domain。

### 歷史中文 msgid 遷移（2026-04 完成）

2026-04 之前專案曾錯誤地將繁體中文當 msgid 使用（`__('課程名稱', 'power-course')`）。已於 `issue/191` 分支一次性重構為英文 msgid，並同步產出 `languages/power-course-zh_TW.po` 保證繁中介面不受影響。重構後**絕對禁止**再引入中文 msgid。

### i18n pipeline 殘缺（2026-04 修復）

2026-04 之前 `pnpm run i18n:build` 只串 `pot → mo → json`，**缺 `.pot → .po` 的合併步驟**，導致：

- 新增的 PHP / JS 字串永遠進不到 `.po`，繁中介面 fallback 顯示英文
- 前台 vanilla TS（`inc/assets/src/`）整個目錄從未被掃到 `.pot`，30+ 條字串無法翻譯

已於本次 PR 修復：

1. `scripts/i18n-make-pot.mjs` 的 `JS_GLOB` 改為 `JS_GLOBS` 陣列，新增 `inc/assets/src/**`
2. 新增 `scripts/build-zhtw-po.mjs` 執行 merge（透過新指令 `pnpm run i18n:merge`）
3. 新增 `scripts/i18n-translations/manual.json` 作為繁中翻譯的 Single Source of Truth
4. `package.json` 的 `i18n:build` 改為 `pot → merge → mo → json`
5. **`vite.config-for-wp.ts` 補上 `@wordpress/i18n` shim alias**（下節詳述）

**自此之後**，禁止手動修改 `languages/*.po` 的 `msgstr` —— 翻譯變動一律走 `scripts/i18n-translations/*.json`，讓 build pipeline 成為 `.po` 的唯一產出路徑。

### 前台 bundle 漏掛 @wordpress/i18n shim（2026-04 修復）

**症狀**：`.pot` / `.po` / `.mo` / `.json` 全部正確、`wp.i18n.__('Collapse', 'power-course')` runtime 能回傳「收合」，但前台頁面 `<p>Collapse</p>` 仍顯示英文。

**根因**：前台 vanilla TS bundle 由 `vite.config-for-wp.ts` 負責打包，而此 config 缺少：

```ts
'@wordpress/i18n': path.resolve(__dirname, 'js/src/shims/wordpress-i18n.ts'),
```

Vite 遇到 `import { __ } from '@wordpress/i18n'` 時若**沒**走 shim，會把 `@wordpress/i18n` 的 npm package 直接打包進 bundle。打包版本會維護**自家的 i18n store**（`defaultI18n`），跟 `window.wp.i18n` 是兩個獨立實例。`Bootstrap.php::inject_locale_data_to_handle()` 只會 `setLocaleData` 到 `window.wp.i18n`，bundle 內的 `__` 永遠讀不到翻譯，於是 fallback 成 msgid 英文。

**關鍵教訓**：任何新的 Vite entry point（新的 config、新的 bundle），**必須**檢查是否已設 `@wordpress/i18n` 的 alias 指向 `js/src/shims/wordpress-i18n.ts`。shim 本體是 `export const __ = (text, domain) => window.wp.i18n.__(text, domain)`，薄薄一層確保 bundle 與 PHP 注入的翻譯 store 共用。

**PR 審查新增確認項**：新增 Vite config / entry 時，需在該 config 的 `resolve.alias` 加 shim 對照。
